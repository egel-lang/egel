/*
   Some bla bla
*/
/*
namespace Test (
    using System

    def concat =
        [ nil, YY -> YY 
        | cons X XX, YY -> cons X (concat XX YY) ]

    def k = j
    def j = i
    def i = [ X, Y:nil -> X |  X:int, Y -> Y ]

    def filter =
        [ nil -> nil
        | cons (X:int) XX -> cons X (filter XX)
        | cons X XX -> filter XX ] 

    object box W H (
        data width, W
        data height, H

        def set_width =
            [ SELF, W -> set SELF width W]
    )

    object circle R (
        data radius, R

        def move =
            [ SELF -> SELF ]
    )

    object simple (
        data const, 1
        def change_const = [ S -> S ]
    )

)
*/

using System
using Test

// def main = 1 + 2

// def main = concat (concat {1, 2, 3} {4, 5, 6}) {}

// def main = get (Test.bx (1 + 2) 5) Test.radius

def main = 1 
