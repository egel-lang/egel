# I increased the grpc message size to 1GB but it still doesn't do this

import "prelude.eg"
import "egel_rpc.ego"

using System
using List

def main = 
    let C = rpc_client "localhost:50001" in
    rpc_call C [_ -> from_to 0 10000 ]
    
